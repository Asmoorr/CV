## 1. Инструменты и команды

- [x] 1.1 Добавить `@playwright/test` как зафиксированную dev-зависимость и установить совместимый Chromium; проверить версии командой Playwright и успешный запуск браузера.
- [x] 1.2 Добавить отдельные npm-команды для визуальной проверки, открытия HTML-отчёта и явного обновления snapshots, сохранив `npm test` за Vitest; проверить, что список скриптов отражает разделение unit и visual проверок.

## 2. Конфигурация воспроизводимого окружения

- [x] 2.1 Создать `playwright.config.ts` с production-подобным Next.js `webServer`, тестовым портом, одним Chromium worker-контекстом, HTML-репортёром и выводом результатов под `/artifacts/`; проверить автоматический старт и остановку сервера тестовым прогоном.
- [x] 2.2 Настроить проекты `desktop-1440x1000`, `tablet-768x1024` и `mobile-320x800` с device scale factor 1, reduced motion и централизованным малым screenshot-допуском; проверить через `--list`, что для обеих локалей формируется шесть проверок.
- [x] 2.3 Обновить правила Git так, чтобы expected snapshots отслеживались, а actual/diff, `test-results` и HTML-отчёт игнорировались; проверить `git status` после неуспешного тестового прогона.

## 3. Визуальная матрица и эталоны

- [x] 3.1 Добавить Playwright spec, который для `/ru` и `/en` ожидает готовности документа и `document.fonts.ready`, скрывает нестабильный кастомный указатель и делает полноэкранное сравнение с читаемым именем локали/viewport; проверить выполнение всех шести комбинаций.
- [x] 3.2 Сгенерировать и добавить в Git первые шесть expected PNG; проверить размеры каждого изображения и визуально просмотреть desktop, tablet и mobile для обеих локалей на переполнение, наложения и некорректные переносы.
- [x] 3.3 Дважды подряд запустить обычную визуальную проверку без изменений; проверить, что оба прогона успешны и не изменяют snapshots.
- [x] 3.4 Внести временное локальное CSS-отклонение, запустить проверку и подтвердить ошибку с expected/actual/diff в HTML-отчёте, затем отменить только временное отклонение и проверить возврат теста к успешному состоянию.

## 4. Документация и итоговая проверка

- [x] 4.1 Дополнить README требованиями к браузеру и командами проверки, просмотра различий и осознанного обновления эталонов; проверить инструкции последовательным выполнением в чистой установке зависимостей.
- [x] 4.2 Выполнить `npm test`, visual test, typecheck, lint и production build; проверить, что все уровни проходят, а рабочее дерево содержит только намеренно добавленные конфигурацию, тест, документацию и expected PNG.
