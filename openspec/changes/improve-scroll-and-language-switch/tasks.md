## 1. Плавная прокрутка

- [x] 1.1 Добавить клиентский контроллер wheel-прокрутки с нормализацией `deltaMode`, ограниченной целевой позицией и одним останавливаемым `requestAnimationFrame`-циклом; проверить отсутствие лишних кадров после остановки и корректные границы документа.
- [x] 1.2 Реализовать адаптивную обработку ввода: мягко объединять дискретные импульсы колеса, не усиливать малые пиксельные события тачпада и быстро гасить остаточную инерцию при смене направления; проверить поведение сериями синтетических wheel-событий разных размеров и частоты.
- [x] 1.3 Исключить двойное сглаживание между CSS и покадровым `scrollTo`, сохранить отдельное корректное поведение якорных ссылок; проверить отсутствие заметной задержки и корректный отступ фиксированной шапки.
- [x] 1.4 Не перехватывать pinch-to-zoom, горизонтально доминирующие жесты и ввод во вложенной прокручиваемой области, способной двигаться дальше; проверить каждый случай браузерным тестом или минимальной тестовой фикстурой.
- [x] 1.5 Добавить Playwright-сценарии для колеса, малых частых событий тачпада, быстрой смены направления и достижения обеих границ страницы; проверить плавность, пропорциональность и отсутствие продолжительного движения после прекращения ввода.

## 2. Интерактивное состояние курсора

- [x] 2.1 Определить единый критерий ненавигационных кликабельных элементов, исключив `header`, `nav`, переключатель языка и fragment-ссылки; проверить выборку на ссылках проекта/контакта и элементах навигации.
- [x] 2.2 Добавить в `FinePointerCursor` управление hover-состоянием с корректной подпиской и очисткой событий; проверить, что состояние включается при входе и сбрасывается при выходе указателя и размонтировании.
- [x] 2.3 Реализовать плавное увеличение самого кольца без layout shift и перехвата кликов, сохранив позиционирование через `requestAnimationFrame`; проверить computed style/состояние кольца в Playwright.
- [x] 2.4 Отключить декоративное увеличение для coarse-pointer и `prefers-reduced-motion`; проверить оба режима эмуляцией media features.
- [x] 2.5 Добавить hover-анимацию самим элементам шапки, меню, переключателя языка и внутренних ссылок, переиспользуя существующие transition/motion-токены; проверить, что кольцо остается базового размера, а анимация элемента не вызывает layout shift и не скрывает `focus-visible`.

## 3. Переключение языка

- [x] 3.1 Удалить перенос текущего hash при построении перехода на другую локаль и обеспечить открытие нового pathname сверху; проверить итоговый URL и `scrollY` после переключения.
- [x] 3.2 Добавить браузерные регрессионные сценарии смены языка из верхней позиции, после прокрутки вниз и после ручного возврата наверх при оставшемся fragment; во всех случаях проверить выбранную локаль и позицию у начала страницы.

## 4. Итоговая проверка

- [x] 4.1 Запустить lint, typecheck и релевантные Vitest/Playwright-тесты, устранить регрессии и подтвердить успешное завершение всех проверок.
- [x] 4.2 Проверить обе локали на desktop с колесом и тачпадом, а также на mobile/coarse-pointer viewport: прокрутка остается управляемой, навигация и клавиатурный focus работают как прежде, курсор не появляется на touch-устройствах, а смена языка не переносит экран вниз.
